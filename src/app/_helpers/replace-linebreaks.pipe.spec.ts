import { ReplaceLinebreaksPipe } from './replace-linebreaks.pipe';

describe('ReplaceLinebreaksPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLinebreaksPipe();
    expect(pipe).toBeTruthy();
  });
});
