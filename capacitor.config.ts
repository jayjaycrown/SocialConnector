import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'club.gary.socialconnector',
  appName: 'Social Connector',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
