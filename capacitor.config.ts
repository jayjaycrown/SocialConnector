import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jayjaycrown.socialconnector',
  appName: 'Social Connector',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
